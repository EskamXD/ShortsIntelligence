import React from "react";
import { Form, Button } from "react-bootstrap";

interface AudioPanelProps {
    files: File[];
}

const AudioPanel: React.FC<AudioPanelProps> = ({ files }) => {
    {
        /* work in progress */
    }
    return <></>;
};

export default AudioPanel;
